services:
  bot:
    build: .
    image: fintracker:sheets
    command: ["poll"]
    restart: unless-stopped
    env_file: .env
    environment:
      FINTRACKER_SHEETS__STATE_PATH: /var/lib/fintracker/sheetbot.sqlite3
    volumes:
      - sheetstate:/var/lib/fintracker
volumes:
  sheetstate:
