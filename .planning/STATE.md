# Состояние новой концепции

Реализован отдельный sheet-only runtime, CLI, Docker Compose, интеграционный Apps Script,
текст/голос, уточнения, сохранение листа и защита от повторных записей.
Локальный архив старой реализации удалён по запросу пользователя.
Подготовлены production Compose и deploy/release.sh с проверкой до переключения.

Внешнее подключение не выполнено: в локальном окружении есть Telegram-токен, но нет
настроенного Google bridge, разрешённых Telegram ID и AI/ASR-конфигурации.
На прежнем сервере AI был настроен по архивному отчёту; фактическое состояние сервера
в этой сессии не проверялось. Сервер и реальная таблица в этой сессии не менялись.

Следующий шаг: настроить Apps Script и окружение по README, выполнить read-only check,
проверить запись на тестовом листе, затем переключить действующий бот с остановкой старых сервисов.

Проверено: 23 Python-сценария и 11 сценариев Google Apps Script; Ruff, форматирование,
mypy strict (21 модуль), сборка wheel/sdist. Живые внешние вызовы не выполнялись.

Запрос на выпуск: удалить архив, push в main, деплой. SSH root@2.26.231.184
отклонён (publickey,password); запрошен актуальный SSH alias/пользователь/ключ.
Production не изменён. make check: 34 теста, Ruff, mypy — успешно.
